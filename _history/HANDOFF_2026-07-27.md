# Spider Forge EvidencePack v2 交接

## 本輪結論

**VERIFIED**：瓶頸同時存在於前期證據、決策模型與 coding model；不能只換模型。

- EvidencePack 已補：redirect/canonical、Browser 403 對照 plain HTTP、JSON response body、POST 遮密、RSS/Atom、自動 detail URL、動態平面資料集與相關 metadata。
- `validation` 現在會進 coding prompt；無可重播 body 時程式硬擋 API，存在結構化文章資料時程式優先 hybrid。
- 常見 Scrapy stack trace 先確定性診斷；已知錯誤不再交 Qwen 猜。
- 全套離線測試：**71/71 passed**。

## Live 證據

| 站點 | 結果 | 真正阻點 |
|---|---|---|
| ECB | escalated，0 items | 舊 run 未保存第 14 個 JSON body；修後 recon 已抓到 `chunk_0.json`、7 個 flat-dataset record，尚未再跑完整 Graph |
| Bank of England | escalated，0 items | 已自動發現 `/rss/news`（20 records）；生成碼使用不存在的 `SelectorList.first()` |
| CNA | 結構 **24/24** 合格；未升版 | Gemini 只接受 9/24（37.5%），整批比例 gate 拒絕 |
| CTEE | 未執行 crawl | DeepSeek 生成 586 行；line 586 未閉合字串，syntax gate 攔截 |
| MoneyDJ | 0 items | 候選語法正確；猜測 `HeadLineNews.axd` 後無資料。Qwen 誤診為 syntax error |

最新候選：

- `news_crawler/_candidates/cna-934552c2/cna_spider.py`
- `news_crawler/_candidates/ctee-e2d657ea/ctee_spider.py`
- `news_crawler/_candidates/moneydj-a3f0ac45/moneydj_spider.py`
- `news_crawler/_candidates/ecb-9c68cc89/ecb_spider.py`
- `news_crawler/_candidates/bankofengland-21526d95/bankofengland_spider.py`

以上皆未通過完整 gate，**不可手動升版**。

## 模型節點判斷

| 節點 | 現況 | 建議 |
|---|---|---|
| Topic Gate | Gemini 已能精準逐 item 判斷 | 保留 Gemini |
| Strategy | Qwen 忽略可重播 JSON，confidence 還輸出 85 | 證據充分時用程式決策；只有模糊案例才呼叫 Gemini |
| Diagnose | Qwen 把 AttributeError/0 items 誤判 robots/syntax | stack/stats 先程式解析；未知錯誤再交 Gemini，這裡換 Gemini 預期改善最大 |
| Generate/Repair | DeepSeek/Kimi 會產 `.first()`、Playwright 濫用或超長破碼 | 不是換 Qwen/Gemini 的問題；先限制行數、依賴與 AST，才評 coding model A/B |

## 下一步（依優先序）

1. Topic Gate 改成「保留 Gemini accept items → 對過濾後 items 重跑品質 gate」，不要因整批相關比例低而丟掉 CNA 的 9 篇合格財經文。
2. 生成碼加 `max_lines`（建議 350）與 forbidden API 靜態檢查；語法錯時只做最小 AST/字串修復，不整支重寫。
3. Diagnosis 擴充 `AttributeError`、HTTP status、0 items、JSON decode、timeout 的 deterministic parser；Qwen/Gemini 只處理 UNKNOWN。
4. 用 `max_retries=0` 各重跑 ECB、BoE：預期 ECB 走 flat JSON hybrid；BoE 走 RSS hybrid。
5. MoneyDJ 先人工唯讀確認 live endpoint/body，再重跑；禁止再依 URL 名稱猜 API。

## 驗證指令

從 `workspace/backend`：

```powershell
..\.venv\Scripts\python.exe -m app.spider_forge_system.tests.test_graph
..\.venv\Scripts\python.exe -m app.spider_forge_system.tests.test_topic_gate
..\.venv\Scripts\python.exe -m app.spider_forge_system.run_batch cna ctee moneydj
```

最後一行會使用正常修復額度；額度緊時，先把 `run_site(..., max_retries=0)`。
